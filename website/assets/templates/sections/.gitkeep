# Section templates saved by the admin dashboard.
# Path: assets/templates/sections/{name}.html
# Each file holds the outerHTML of a section the admin "Save as template"d.
# The "Insert" + "Replace with template" actions in the page editor list
# every .html in this folder.
